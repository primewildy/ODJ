[package]
name = "ui"
edition.workspace = true
version.workspace = true

[dependencies]
audio = { path = "../audio" }
decode = { path = "../decode" }
control = { path = "../control" }
analysis = { path = "../analysis" }
eframe = { workspace = true }
egui_extras = { workspace = true }
anyhow = { workspace = true }
libc = { workspace = true }
stems = { path = "../stems" }
serde = { workspace = true }
toml = { workspace = true }
# Network-output ring buffer (UPnP HTTP stream). Same SPSC the audio
# crate uses; same dep already in the workspace.
rtrb = { workspace = true }
